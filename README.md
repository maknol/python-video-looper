Small script to play videos from a folder to infinity loop.

You will need python, moviepy 2.2.1 (pip install moviepy), pygame 2.6.0 (pip install pygame), and ffplay (part of ffmpeg https://www.ffmpeg.org/ ).
